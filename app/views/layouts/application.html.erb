<!DOCTYPE html>
<html >
  <head>
    <title>PAF</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag "favicon.png", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "animate.min.css", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
  </head>
  <body class="font-quicksand dark:bg-slate-900 bg-white">
    <%= render 'layouts/navbar' %>
    <main class="container mx-auto mt-10">
      <%= yield %>
      <%= render 'layouts/flash' %>
      <div id="frame-preloader" class="hidden">
        <div class="spinner-wave fixed top-0 left-0 z-50 w-screen h-screen  justify-center items-center bg-black bg-opacity-40  pointer-events-none">
          <div class="spinner-wave-dot spinner-dot-intermittent"></div>
          <div class="spinner-wave-dot spinner-dot-intermittent"></div>
          <div class="spinner-wave-dot spinner-dot-intermittent"></div>
          <div class="spinner-wave-dot spinner-dot-intermittent"></div>
        </div>
      </div>
    </main>
  </body>
</html>
